# Media

Place source media for the micro-project in this directory.

For a deterministic local sample, run from the exercise root:

```bash
scripts/create-sample-source.sh media/source.mp4 60
```

Generated HLS playlists and segments are written under `player/streams/`, not here.
